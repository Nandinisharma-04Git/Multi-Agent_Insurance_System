apiVersion: v1
kind: Service
metadata:
  name: insurance-app-service
  labels:
    app: insurance-app
spec:
  type: NodePort
  selector:
    app: insurance-app
  ports:
  - port: 5000
    targetPort: 5000
    nodePort: 30080
